 
 - Day 1
    - 13. Roman to Integer
        Input: s = "LVIII"
        Output: 58
        Explanation: L = 50, V= 5, III = 3.

        solution - loop from end value less than prev then subtract

    -  **IMPORTANT** Minimum Window Substring **IMPORTANT**
        Input: s = "ADOBECODEBANC", t = "ABC"
        Output: "BANC"
        Explanation: The minimum window substring "BANC" includes 'A', 'B', and 'C' from string t.

        solution - required map and have map solve patience, if have found move left and reduce have --
        as it need substring so store start index and length

    - Top K Frequent Elements
        Input: nums = [1,1,1,2,2,3], k = 2
        Output: [1,2]

        Solution - map.keySet() priority queue a[1]-b[1]

    - 3 Sum
        Solution - solve by 2 sum and inner one left right and sum < 0 , sum > 0 move right -- left ++

    - Valid Anagram
    Soluton int[] a = new int[26] ,, int[charAt - 'a']

    - Reverse Linked List
    Solution  next = curr.next  curr.prev = next ;prev = curr , curr = next , 

    - Valid Paranthesis 
    Solution stack add when not , else if stack 0 or pop != curr then return false 

    - Longest Substring Without Repeating Characters

    - Count Elements With Maximum Frequency
    Solution - map count > max -> save new ans , if same then add to ans

    - **IMPORTANT** Maximum Sum of Distinct Subarrays With Length K **IMPORTANT**
    Solution - store in set , if contains reduce and move left as that sequence will not be there 
    also if found max then move left to find next 

    - **IMPORTANT** Merge k Sorted Lists **IMPORTANT**
        Input: lists = [[1,4,5],[1,3,4],[2,6]]
        Output: [1,1,2,3,4,4,5,6]
        Explanation: The linked-lists are:
        [
        1->4->5,
        1->3->4,
        2->6
        ]
        merging them into one sorted linked list:
        1->1->2->3->4->4->5->6
        
        Solution -> take pq a.val - b.val , store each head in pq then while pq size and poll and add to dummy next

    - **IMPORTANT** 146. LRU Cache **IMPORTANT**
    Sol -> head,tail,addToTail, removeNode, get-> addtoTail, 
            put-> if exist update & add tail, checkCapacity else remove headnext, create and add 
          
- Day 2

- 567. Permutation in String
    Example 1:

    Input: s1 = "ab", s2 = "eidbaooo"
    Output: true
    Explanation: s2 contains one permutation of s1 ("ba").

    SOLUTION -- Understand question its permutation not palindrome , So if abc -> it can be bca
            -- Sliding window and compare int[]