Emirates
Emirates NBD's engineering challenges — real-time transactions, fraud detection,
personalisation at 20 million users — are exactly the kind of problems I want to work on next."
Angular vs React


CISCO
eventpropagationa dn related quesiton

- System design revision
  Design Rate Limit -> Queue and allowrequest() -> cleanUpExpireFromQueue(),check if queue > max fale else true
  LRU cache -> create double linked list , add move to tail and remove from list ,  HashMap<Integer, Node> map;
            -> if put update the map if there and return, if max size queue then remove and map remove key, 
               put and add to tail in lru 
            -> first check if not then -1 **IMPORTANT** for error case check, get remove from node and add to tail
            -> bfs add list in queue and poll and for if size fix
            -> dfs can solve by bfs and recursion both 

DSA revision
JS polyfill and practice revision
Notes revision
BASIC COMPUTER NETWORK
BASIC LINUX

